spring:
  application:
    name: jme-pcs-it
  main:
    banner-mode: off
  docker:
    compose:
      enabled: true
      file: ../docker/docker-compose.yml
      host: 172.18.0.1
      lifecycle-management: start-and-stop
      skip:
        in-tests: false
      arguments:
        - "--project-name=${COMPOSE_PROJECT_NAME:jme-pcs-it}"
      readiness:
        timeout: 60s
